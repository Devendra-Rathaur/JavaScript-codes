let first = document.getElementById('first')
// first.insertAdjacentHTML('beforebegin','<div>before bigin</div>')
// first.insertAdjacentHTML('afterbegin','<div>after bigin</div>')

// first.insertAdjacentHTML('beforeend','<div>before end</div>')

// first.insertAdjacentHTML('afterend', '<div>after end</div>')
// first.remove()

